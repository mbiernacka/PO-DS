package agh.ics.oop;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class AnimalTest {
    /*
    @Test
    void OrientationTest(){
        Animal animal1 = new Animal();
        animal1.move(MoveDirection.RIGHT);
        assertEquals(MapDirection.EAST, animal1.getOrientation());
    }

    @Test
    void PositionTest(){
        Animal animal2 = new Animal();
        animal2.move(MoveDirection.RIGHT);
        animal2.move(MoveDirection.FORWARD);
        Vector2d expected = new Vector2d(3,2);
        assertEquals(expected, animal2.getPosition());
    }

    @Test
    void MapBoundariesTest(){
        Animal animal3 = new Animal();
        animal3.move(MoveDirection.FORWARD);
        animal3.move(MoveDirection.RIGHT);
        animal3.move(MoveDirection.FORWARD);
        animal3.move(MoveDirection.FORWARD);
        animal3.move(MoveDirection.FORWARD);
        assertTrue(animal3.getPosition().precedes(new Vector2d(4,4)));
    }*/
}
